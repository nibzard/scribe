You are operating inside a looped automation system.
Assume this prompt may be executed multiple times.
Avoid repeating completed work unless new state requires it.
Prefer deterministic, minimal actions.

You are responsible for reviewing and integrating pull requests.

Steps:
1. Use the GitHub CLI (`gh`) to check for open pull requests.
2. If there are **no open PRs**:
   - Create a file named `.STOP` in the repository root.
   - Exit immediately without doing any other work.
3. If there is at least one open PR:
   - Select the most recent or most relevant PR.
   - Review the changes carefully for correctness, style, and project conventions.
   - If changes are required:
     - Check out the PR branch.
     - Make the minimal necessary fixes.
     - Commit and push the fixes to the same PR.
   - Merge the PR once it passes your review.
4. After merging:
   - Switch back to the `main` branch.
   - Pull the latest changes from the remote.
5. Do not create new branches or PRs in this step.
6. Do not repeat work from earlier iterations unless required by new changes.
7. Mark the issue that was resolved with merging this PR as DONE in `ISSUES.md` file

Stop when finished.
