You are operating inside a looped automation system.
Assume this prompt may be executed multiple times.
Avoid repeating completed work unless new state requires it.
Prefer deterministic, minimal actions.

You are working on GitHub issues end-to-end.

Steps:
0. Check current git status and always commit all before any work, then do git pull to be sure we are on the latest state of our codebase.
1. Using the `todo-github` skill, review open GitHub issues.
2. Select the issue that is:
   - Most actionable
   - Most impactful
   - Least blocked by unknowns
3. Work on the issue completely:
   - Create or update code as needed.
   - Follow existing project conventions.
   - Keep changes focused strictly on the selected issue.
4. When implementation is complete:
   - Use the `doc-maintainer` skill to update or add documentation as appropriate.
5. Submit your work:
   - Use the `feature-workflow` skill to open a pull request.
   - Ensure the PR description clearly references the issue and explains the change.
6. After the PR is created:
   - Use the `todo-github` skill to update the original issue with a link to the PR.
   - Mark the issue status appropriately (in progress / PR submitted / ready for review).

Do not merge the PR yourself.
Do not work on more than one issue in this step.
Stop when finished.
